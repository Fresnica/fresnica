diff --git a/.github/workflows/mobile-native-signing.yml b/.github/workflows/mobile-native-signing.yml
index f001712..e0446a4 100644
--- a/.github/workflows/mobile-native-signing.yml
+++ b/.github/workflows/mobile-native-signing.yml
@@ -30,7 +30,7 @@ jobs:
           fi
           test -x "$SDKMANAGER"
           yes | "$SDKMANAGER" --licenses >/dev/null 2>&1 || true
-          "$SDKMANAGER" "ndk;${ANDROID_NDK_VERSION}" "platforms;android-34"
+          "$SDKMANAGER" "ndk;${ANDROID_NDK_VERSION}" "platforms;android-34" "build-tools;36.0.0"
       - name: Install cargo-ndk
         run: cargo install cargo-ndk --version 4.1.2 --locked
       - name: Build Rust Android package and generated Kotlin
@@ -38,9 +38,13 @@ jobs:
           export ANDROID_NDK_HOME="$ANDROID_HOME/ndk/$ANDROID_NDK_VERSION"
           export ANDROID_NDK_ROOT="$ANDROID_NDK_HOME"
           bash bindings/mobile/scripts/build-android.sh
+      - uses: actions/setup-java@v4
+        with:
+          distribution: "temurin"
+          java-version: "17"
       - uses: gradle/actions/setup-gradle@v4
         with:
-          gradle-version: "7.5.1"
+          gradle-version: "9.4.1"
       - name: Build and lint Android AAR
         run: |
           gradle -p bindings/mobile/platform/android assembleRelease lintRelease --stacktrace
diff --git a/.github/workflows/native-sdk-platform-packaging.yml b/.github/workflows/native-sdk-platform-packaging.yml
index f8491b3..60a4767 100644
--- a/.github/workflows/native-sdk-platform-packaging.yml
+++ b/.github/workflows/native-sdk-platform-packaging.yml
@@ -26,7 +26,7 @@ jobs:
           fi
           test -x "$SDKMANAGER"
           yes | "$SDKMANAGER" --licenses >/dev/null 2>&1 || true
-          "$SDKMANAGER" "ndk;${ANDROID_NDK_VERSION}" "platforms;android-34"
+          "$SDKMANAGER" "ndk;${ANDROID_NDK_VERSION}" "platforms;android-34" "build-tools;36.0.0"
       - name: Install cargo-ndk
         run: cargo install cargo-ndk --version 4.1.2 --locked
       - name: Build Rust libraries and generated Kotlin
@@ -34,9 +34,13 @@ jobs:
           export ANDROID_NDK_HOME="$ANDROID_HOME/ndk/$ANDROID_NDK_VERSION"
           export ANDROID_NDK_ROOT="$ANDROID_NDK_HOME"
           bash bindings/native/scripts/build-android.sh
+      - uses: actions/setup-java@v4
+        with:
+          distribution: "temurin"
+          java-version: "17"
       - uses: gradle/actions/setup-gradle@v4
         with:
-          gradle-version: "7.5.1"
+          gradle-version: "9.4.1"
       - name: Build and lint Native SDK AAR
         run: gradle -p bindings/native/platform/android assembleRelease lintRelease --stacktrace
       - name: Validate Native SDK AAR
diff --git a/.github/workflows/native-sdk-release.yml b/.github/workflows/native-sdk-release.yml
index 04f39eb..a36a9a8 100644
--- a/.github/workflows/native-sdk-release.yml
+++ b/.github/workflows/native-sdk-release.yml
@@ -144,7 +144,7 @@ jobs:
           fi
           test -x "$SDKMANAGER"
           yes | "$SDKMANAGER" --licenses >/dev/null 2>&1 || true
-          "$SDKMANAGER" "ndk;${ANDROID_NDK_VERSION}" "platforms;android-34"
+          "$SDKMANAGER" "ndk;${ANDROID_NDK_VERSION}" "platforms;android-34" "build-tools;36.0.0"
       - name: Install cargo-ndk
         run: cargo install cargo-ndk --version 4.1.2 --locked
       - name: Build Rust libraries and generated Kotlin
@@ -152,9 +152,13 @@ jobs:
           export ANDROID_NDK_HOME="$ANDROID_HOME/ndk/$ANDROID_NDK_VERSION"
           export ANDROID_NDK_ROOT="$ANDROID_NDK_HOME"
           bash bindings/native/scripts/build-android.sh
+      - uses: actions/setup-java@v4
+        with:
+          distribution: "temurin"
+          java-version: "17"
       - uses: gradle/actions/setup-gradle@v4
         with:
-          gradle-version: "7.5.1"
+          gradle-version: "9.4.1"
       - name: Build and lint Native SDK AAR
         run: gradle -p bindings/native/platform/android assembleRelease lintRelease --stacktrace
       - name: Validate Android Native SDK
diff --git a/.github/workflows/react-native-adapter.yml b/.github/workflows/react-native-adapter.yml
index 1b9b816..b3f274c 100644
--- a/.github/workflows/react-native-adapter.yml
+++ b/.github/workflows/react-native-adapter.yml
@@ -17,6 +17,20 @@ jobs:
       - uses: actions/setup-node@v4
         with:
           node-version: "22"
+      - uses: actions/setup-java@v4
+        with:
+          distribution: "temurin"
+          java-version: "17"
+      - uses: gradle/actions/setup-gradle@v4
+        with:
+          gradle-version: "9.4.1"
+      - name: Configure Android adapter with Gradle 9.4.1
+        run: |
+          touch /tmp/fresnica-native-sdk.aar
+          gradle -p adapters/react-native/android help \
+            -PfresnicaNativeSdkAar=/tmp/fresnica-native-sdk.aar \
+            -PfresnicaReactNativeVersion=0.74.2 \
+            --stacktrace
       - name: Check Apple adapter build script syntax
         run: bash -n adapters/react-native/apple/build.sh
       - name: Test adapter contract and one-time build tooling
diff --git a/adapters/react-native/android/build.gradle b/adapters/react-native/android/build.gradle
index 7f0e259..361b8ab 100644
--- a/adapters/react-native/android/build.gradle
+++ b/adapters/react-native/android/build.gradle
@@ -1,6 +1,5 @@
 plugins {
-    id 'com.android.library' version '7.4.0'
-    id 'org.jetbrains.kotlin.android' version '1.9.24'
+    id 'com.android.library' version '9.2.1'
 }
 
 String requiredProperty(String name) {
@@ -32,9 +31,6 @@ android {
         targetCompatibility JavaVersion.VERSION_1_8
     }
 
-    kotlinOptions {
-        jvmTarget = '1.8'
-    }
 }
 
 repositories {
diff --git a/bindings/mobile/PACKAGING.md b/bindings/mobile/PACKAGING.md
index 6f40319..73c9725 100644
--- a/bindings/mobile/PACKAGING.md
+++ b/bindings/mobile/PACKAGING.md
@@ -6,6 +6,8 @@ This document describes the reproducible outputs produced from `fresnica-mobile-
 
 The Xaman-derived Android application currently targets API 26+ and builds four ABIs. Fresnica keeps that compatibility baseline for the first integration:
 
+The Android AAR build on `main` is validated with JDK 17, Gradle 9.4.1 and Android Gradle Plugin 9.2.1. AGP 9 built-in Kotlin compiles the generated UniFFI Kotlin sources.
+
 | Android ABI | Rust target |
 | --- | --- |
 | `armeabi-v7a` | `armv7-linux-androideabi` |
diff --git a/bindings/mobile/platform/android/build.gradle b/bindings/mobile/platform/android/build.gradle
index 59d22fc..74efc3f 100644
--- a/bindings/mobile/platform/android/build.gradle
+++ b/bindings/mobile/platform/android/build.gradle
@@ -1,6 +1,5 @@
 plugins {
-    id 'com.android.library' version '7.4.0'
-    id 'org.jetbrains.kotlin.android' version '1.9.24'
+    id 'com.android.library' version '9.2.1'
 }
 
 android {
@@ -14,11 +13,7 @@ android {
 
     sourceSets {
         main {
-            java.srcDirs += [
-                'src/main/java',
-                'src/main/kotlin',
-                '../../build/android/kotlin'
-            ]
+            kotlin.directories.add('../../build/android/kotlin')
             jniLibs.srcDirs = ['../../build/android/jniLibs']
         }
     }
@@ -28,9 +23,6 @@ android {
         targetCompatibility JavaVersion.VERSION_1_8
     }
 
-    kotlinOptions {
-        jvmTarget = '1.8'
-    }
 }
 
 dependencies {
diff --git a/bindings/native/PACKAGING.md b/bindings/native/PACKAGING.md
index 83502f7..34b5e60 100644
--- a/bindings/native/PACKAGING.md
+++ b/bindings/native/PACKAGING.md
@@ -17,6 +17,8 @@ Supported baseline:
 
 Minimum Android API: 26.
 
+Source packaging on `main` is validated with JDK 17, Gradle 9.4.1 and Android Gradle Plugin 9.2.1. AGP 9 built-in Kotlin is used; the generated UniFFI Kotlin directory is registered as a Kotlin source directory rather than a Java source directory.
+
 Build the Rust libraries and generated Kotlin:
 
 ```sh
diff --git a/bindings/native/platform/android/build.gradle b/bindings/native/platform/android/build.gradle
index 29ebf1d..1fa593e 100644
--- a/bindings/native/platform/android/build.gradle
+++ b/bindings/native/platform/android/build.gradle
@@ -1,6 +1,5 @@
 plugins {
-    id 'com.android.library' version '7.4.0'
-    id 'org.jetbrains.kotlin.android' version '1.9.24'
+    id 'com.android.library' version '9.2.1'
 }
 
 android {
@@ -14,7 +13,7 @@ android {
 
     sourceSets {
         main {
-            java.srcDirs += ['../../build/android/kotlin']
+            kotlin.directories.add('../../build/android/kotlin')
             jniLibs.srcDirs = ['../../build/android/jniLibs']
             manifest.srcFile 'src/main/AndroidManifest.xml'
         }
@@ -25,9 +24,6 @@ android {
         targetCompatibility JavaVersion.VERSION_1_8
     }
 
-    kotlinOptions {
-        jvmTarget = '1.8'
-    }
 }
 
 dependencies {
diff --git a/bindings/native/tests/android-consumer/build.gradle b/bindings/native/tests/android-consumer/build.gradle
index d6f8cde..72bfcdf 100644
--- a/bindings/native/tests/android-consumer/build.gradle
+++ b/bindings/native/tests/android-consumer/build.gradle
@@ -1,6 +1,5 @@
 plugins {
-    id 'com.android.application' version '7.4.0'
-    id 'org.jetbrains.kotlin.android' version '1.9.24'
+    id 'com.android.application' version '9.2.1'
 }
 
 repositories {
@@ -25,9 +24,6 @@ android {
         targetCompatibility JavaVersion.VERSION_1_8
     }
 
-    kotlinOptions {
-        jvmTarget = '1.8'
-    }
 }
 
 def nativeSdkAar = System.getenv('FRESNICA_NATIVE_AAR')
