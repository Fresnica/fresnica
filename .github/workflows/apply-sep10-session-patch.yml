name: Reapply live SEP-10 session patch

on:
  push:
    branches:
      - dev/sep10-session

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev/sep10-session
          fetch-depth: 0
      - name: Apply staged source patch
        run: |
          git apply --check .ci/sep10-session.patch
          git apply .ci/sep10-session.patch
          rm .ci/sep10-session.patch
          rm .github/workflows/apply-sep10-session-patch.yml
          git diff --check
          bash clients/rust-cli/scripts/validate-sdk-boundary.sh
      - name: Commit source changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Establish SEP-10 authentication sessions"
          git push origin HEAD:refs/heads/dev/sep10-session
