# Desktop Platform References

Desktop products consume the same Application Capability/Core contracts while choosing platform-native packaging, storage and system authorization.

- [Desktop SDK contract](sdk-contract.md)

The macOS Native SDK path is already validated. Windows/Linux public packaging should follow a concrete direct-consumer language/API requirement rather than publishing an undefined bare native library as a complete SDK.
