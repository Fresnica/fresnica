# Network configuration

Fresnica networks are runtime configuration, not wallet identity.

```toml
[network]
default = "testnet"
```

Available networks:

- mainnet
- testnet

The same Stellar account can be used on different networks.
